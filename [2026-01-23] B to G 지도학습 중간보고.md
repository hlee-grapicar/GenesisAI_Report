# Blender to Genesis 지도학습 중간보고

## MLP 구조
- 입력층 25개 노드 
- 은닉층 3개층 (512, 256, 128개 노드), ELU 활성화 함수
- 출력층 2개 노드 (스티어링, 쓰로틀), Tanh 활성화 함수

- 최적화 알고리즘(Optimizer) : Adam
- 학습률 : 0.001
- 배치 사이즈 : 128
- 학습 횟수 : 20,000회
- 손실 함수 : 가중치 적용 평균 제곱 오차, (스티어링 * 5, 쓰로틀 * 1)
 $Loss = \frac{1}{N} \sum ( (Pred_{steer} - Target_{steer})^2 \cdot 5.0 + (Pred_{throttle} - Target_{throttle})^2 \cdot 1.0 )$

- 입력 :
  
| 입력 변수 항목 | 계산/구성 방식 | 비고 |
| :--- | :--- | :--- |
| **중력 벡터 (projected_gravity)** | 3차원 벡터 | 자동차 로컬 좌표계 기준 중력 |
| **조향 위치 (steer_pos)** | 2차원 값 | 현재 조향 조인트 각도 (정규화) |
| **바퀴 위치 더미 (wheel_pos_dummy)** | 4차원 값 | 사용되지 않음, 0으로 채움 |
| **조인트 각속도 (dof_vel)** | 6차원 값 | 조향(2) + 바퀴(4) 조인트 각속도 (정규화) |
| **이전 명령 (actions)** | 2차원 값 | 이전 타임스텝의 조향 및 스로틀 명령 |
| **현재 위치 오차 (remapped_pos_error)** | 3차원 벡터 | 목표 지점까지의 오차 (전방, 좌우, 상하) |
| **미래 위치 오차 (future_remapped_error)** | 3차원 벡터 | 미래 목표 지점까지의 예측 오차 |
| **전문가 명령 (target_expert_action)** | 2차원 값 | 전문가 주행 데이터의 조향 및 스로틀 명령 |
| **총합** | 25 | 전체 입력 차원 수 |

- 출력 :
- 
| 출력 변수 항목 | 차원 | 설명 |
| :--- | :--- | :--- |
| **조향 명령 (steer)** | 1 | 예측된 조향 명령 (정규화된 값) |
| **스로틀 명령 (throttle)** | 1 | 예측된 스로틀(가속) 명령 (정규화된 값) |
| **총합** | 2 | 전체 출력 차원 수 |

## 축 변환

| 변환 항목 | 변환 로직        | 설명                                              |
| :---      | :---             | :---                                              |
| **위치 (Pos)**   | X → Y, Y → -X   | Genesis 시뮬레이터 전방(Z축 -90°) 기준 축 정렬   |
| **속도 (Vel)**   | X → Y, Y → -X   | 위치 좌표계와 물리적 일관성 유지를 위한 동기화    |
| **자세 (Quat)**  | Z-axis -90° 연산 | 전방 지향성 보정을 위한 쿼터니언 회전 연산 적용   |
| **조향 (Steer)** | Sign Inversion   | URDF 물리 엔진 규격 정합을 위한 부호 반전 (-1.0)  |
| **가속 (Thr)**   | Scalar Scaling   | 하이 레벨 가속 제어값을 바퀴 각속도로 매핑 (1500) |

## 성능 최적화를 위한 시스템 튜닝

| 튜닝 항목          | 설정값    | 목적 및 기대 효과                                   |
| :---               | :---      | :---                                              |
| **최소 가속 임계치**   | 0.08        | 정지 마찰력을 극복하고 전문가 속도에 즉각 반응 유도 |
| **조향 가중치 강화**   | 5.0 (Loss)  | 코너링 이탈 방지 및 전문가의 날카로운 조향 복제     |
| **좌우 오차 집중도**   | 15.0 (Obs)  | 현재 위치에서 경로 복귀 시 조향 우선순위 극대화     |
| **예측 제어 (Look)**  | 5 Frames    | 약 0.1~0.2초 앞의 경로를 미리 읽어 예견 주행 구현   |
| **데이터 노이즈 추가** | 0.4 (Std)   | 훈련 데이터에 임의 오차(Noise)를 섞어 회복 능력 강화|
| **서스펜션 PD 제어**   | 2000/100    | 급커브 및 가속 시 차체 흔들림(Oscillation) 방지     |
| **지연 보정 학습**     | i - 1       | 현재 상태를 직전 액션과 매칭하여 물리 딜레이 반영   |
| **휠 속도 스케일링**   | 0.05        | 관측 데이터 폭주를 방지하여 신경망 수렴 안정화      |
| **스폰 높이 보정**     | 0.5m        | 초기 시뮬레이션 시작 시 지면 간섭 및 튕김 현상 제거 |

## 한계
- 다양한 튜닝 시도를 통해 현재의 상태까지 도달
- 세밀한 튜닝을 통해 Blender 환경과 최대한 동일하게 만들고자 노력하였으나, 정밀하게 동일한 환경을 구현하는 것은 지도학습 방식으로는 어려울 것으로 판단됨

## 시행 영상

- 학습 결과 

https://github.com/user-attachments/assets/00efdf35-2c07-4ace-86c8-faabd915ae7d

- 학습하지 않음(쓰로틀, 스티어링 값을 그대로 실행)

https://github.com/user-attachments/assets/b5695f90-c856-462b-8dee-864bd4258927

- 학습된 MLP를 사용한 유사 경로
 
https://github.com/user-attachments/assets/6a3239c2-f68c-4acf-99fb-3286c08f68e3

- 학습된 MLP를 사용하지 않은 유사 경로(쓰로틀, 스티어링 값을 그대로 실행)

https://github.com/user-attachments/assets/9bdc9d28-9ba2-49be-a6d6-7e194ebc11a5


