# Blender to Genesis 지도학습

Blender의 차량 주행을 지도학습을 통해 Genesis에 구현

## 차량 세팅
### 공통 사항(동일한 값 설정)
- body mass : 1320kg
- tire friction : 2.0
- 조향 한계 : 35도
- 바퀴 크기 : 0.335m
- 2WD 후륜구동
### 차이
Blender와 Genesis의 엔진, 단위가 다르기에 실험적 결과로 얻은 값들
- 타이어 무게(b:220, g:25)
- 조향 제어 (g: kp:10,000 / kd:1,000)
- 서스펜션 제어 (g: kp:50,000 / kd:3,000)
- 가속 스케일(g: 1,500)
- 조향 스케일(g: 0.6)

---

## Blender 주행 추출 데이터
| 분류 | 항목명 | 설명 |
| :--- | :--- | :--- |
| **시간** | `time` | 시뮬레이션 경과 시간 (단위: 초) |
| **위치** | `g_pos_x, y, z` | 세계 좌표계 기준 차량 위치 (m) |
| **자세** | `g_qw, qx, qy, qz` | 차량의 회전 상태 (Quaternion) |
| **선속도** | `g_lin_vx, vy, vz` | 세계 좌표계 기준 선속도 (m/s) |
| **각속도** | `g_ang_vx, vy, vz` | 세계 좌표계 기준 각속도 (rad/s) |
| **관측 조향** | `act_steer_norm` | Blender 조향값 (-1.0: 좌, 1.0: 우) |
| **관측 가속** | `act_throttle_norm` | Blender 가속값 (-1.0: 후진, 1.0: 전진) |
| **기타** | `v_long` | 차량 진행 방향(종방향) 속도 성분 |

---

## Blender to Genesis 축 변환
### 1. 전역 좌표계 변환 (CSV → Genesis World)
Blender 주행 데이터의 좌표계를 Genesis 시뮬레이터 환경에 맞추기 위해 Z축 기준 -90도 회전(Clockwise) 적용

| 항목 | 변환 로직 (코드) | 설명 |
| :--- | :--- | :--- |
| **위치 (Position)** | `X_gen = Y_csv`, `Y_gen = -X_csv` | 평면상 -90도 회전 매핑 |
| **속도 (Velocity)** | `VX_gen = VY_csv`, `VY_gen = -VX_csv` | 선속도/각속도 벡터 동일 회전 적용 |
| **자세 (Quaternion)** | `zr = -0.7071` 회전 값 적용 | 쿼터니언에 -90도 회전 성분 합성 |

### 2. 제어 모멘트 및 부호 보정 (Action Mapping)
AI 모델의 출력값(`action`)을 실제 차량의 관절(`joint`) 제어 신호로 변환할 때의 규칙

| 제어 항목 | 보정 로직 | 이유 |
| :--- | :--- | :--- |
| **조향 (Steer)** | `target = -1.0 * action` | AI 표준(Negative=Left)과 URDF 표준(Positive=Left) 일치 |
| **가속 (Throttle)** | `vel = throttle * R * scale` | 각속도(`rad/s`) 제어를 위한 선형 스케일링 |
| **전진 방향** | `Body -Y` 방향 | Corvette 모델 규격상 -Y 축이 정면 방향임 |

### 3. 관측값(Observation) 구성 시 오차 계산
차량의 로컬 좌표계(Body Frame) 기준으로 오차를 산출할 때의 방향성

| 오차 항목 | 계산 방식 | 비고 |
| :--- | :--- | :--- |
| **전방 오차 (Ahead)** | `-pos_error_body.y` | 차량의 정면(-Y) 기준 거리 오차 |
| **좌우 오차 (Right)** | `-1.0 * pos_error_body.x` | 차량의 우측(+X) 기준 편차 |
| **조향 상태 관측** | `-1.0 * joint_pos` | AI가 인식하는 조향 방향과 부호 동기화 |

---

## MLP 구조

### 1. 신경망 아키텍처 (MLP Structure)
| 계층 (Layer) | 구성 | 비고 |
| :--- | :--- | :--- |
| **Input Layer** | 25 nodes | 관측값(Observation) 입력 |
| **Hidden Layer 1** | 512 nodes + ELU | 비선형 특징 추출 |
| **Hidden Layer 2** | 256 nodes + ELU | 특징 차원 축소 |
| **Hidden Layer 3** | 128 nodes + ELU | 최종 특징 요약 |
| **Output Layer** | 2 nodes + Tanh | 조향(Steer), 가속(Throttle) 출력 |

### 2. 학습 세팅 (Hyperparameters)
| 항목 | 설정값 | 설명 |
| :--- | :--- | :--- |
| **입력 차원** | 25 | 중력, 관절 상태, 오차, 미래 예측 등 |
| **출력 차원** | 2 | 조향(-1~1), 가속(0~1) |
| **Optimizer** | Adam | lr=1e-3 (고정 학습률) |
| **Loss Function** | Weighted MSE | 조향 오차에 5.0배 가중치 부여 |
| **Batch Size**| 128 | 미니 배치 학습 크기 |
| **Epochs** | 20,000 | 수렴 성능을 극대화한 반복 횟수 |

### 3. 활성화 함수 (Activation)
*   **ELU (Exponential Linear Unit)**: 은닉층에서 사용. 0 이하 값에서도 그래디언트 소실을 방지하여 부드러운 학습 유도.
*   **Tanh (Hyperbolic Tangent)**: 출력층에서 사용. AI의 출력을 물리 엔진의 입력 범위인 `-1.0 ~ 1.0` 사이로 고정.

---

## 학습 전략 및 특이사항
*   **상태 공간 전처리 (Input Engineering)**:
    - 데이터의 전역 좌표를 차량의 로컬 좌표(Body Frame)로 실시간 변환하여 학습 부하를 줄임
    - 현재 오차뿐만 아니라 **5프레임 뒤의 미래 오차**를 입력받아 '코너 선진입' 거동을 학습
*   **라벨 노이즈 보정 (Label Cleaning)**:
    - 주행 중 마찰력을 이기지 못하는 미세한 데이터 가속값은 **최소 임계값(0.08)**으로 보정하여 '멈추지 않는 주행'을 유도
*   **회복성 강화 (Recovery Training)**:
    - 학습 데이터 구성 시 임의의 위치 오차(Positional Noise)를 시뮬레이션하여, 경로를 이탈했을 때 스스로 중앙으로 복귀할 수 있는 능력을 강화

---

## 입력값(관측값)

| 순번 | 항목 명칭 | 차원 | 세부 내용 및 전처리 |
| :--- | :--- | :---: | :--- |
| **1-3** | **중력 벡터 (Gravity)** | 3 | 차량 로컬 좌표계 기준 중력 방향 (경사도 감지) |
| **4-9** | **관절 위치 (Joint Pos)** | 6 | 앞바퀴 조향각(2) 및 뒷바퀴 회전값(4, 더미) |
| **10-15** | **관절 속도 (Joint Vel)** | 6 | 조향 및 주행축의 회전 속도 (0.05 스케일링 적용) |
| **16-17** | **이전 행동 (Last Action)** | 2 | 직전 단계에서 AI가 내린 [조향, 가속] 명령값 |
| **18-20** | **현재 위치 오차** | 3 | 타겟 지점과의 [전방, 우측, 높이] 오차 (가중치: 5, 15, 1) |
| **21-23** | **미래 위치 오차** | 3 | 5프레임 뒤 타겟과의 [전방, 우측, 높이] 오차 (가중치: 2, 5, 0.5) |
| **24-25** | **정답 데이터** | 2 | 현재 타겟 지점에서의 정답 데이터 [조향, 가속] 값 |

---

## 출력값

| 항목 | 출력 범위 | 물리적 변환 및 적용 |
| :--- | :---: | :--- |
| **조향 (Steer)** | -1.0 ~ 1.0 | **변환**: `action * -0.6 (rad)`<br>**효과**: 왼쪽(-) 끝에서 오른쪽(+) 끝까지 약 35도 범위 제어 |
| **가속 (Throttle)** | -1.0 ~ 1.0 | **변환**: `action * scale (rad/s)`<br>**효과**: 바퀴의 위상 속도를 조절하여 전진 및 후진 동력 생성 |

* 조향 보정(scale) : 0.6
* 속도 보정(scale) : 1,500
* Blender와 Genesis 간의 물리 엔진 차이로 인해 나타나는 오차를 줄이기 위해 실험적으로 얻은 값
---

## 손실 함수
차량의 조향(Steering) 정밀도가 주행 안정성에 직결, 단순히 전체 오차를 줄이는 대신 **조향 오차에 더 높은 페널티**를 부여

#### 1. 수학적 정의 (Formula)
$$Loss = \frac{1}{N} \sum_{i=1}^{N} \left( W_{steer} \cdot (y_{s} - \hat{y}_{s})^2 + W_{throt} \cdot (y_{t} - \hat{y}_{t})^2 \right)$$
*   $y, \hat{y}$: 블렌더에서 자동차에 내린 순수 명령값(조향, 가속). 제네시스와 블렌더의 물리 엔진 환경이 다르기에 물리엔진의 환경이 적용된 실제 작동 값 보다는 순수 명령 값이 물리엔진이 다른 환경에서 더 적합할 것이라 판단하여 순수 명령값으로 진행.
*   $W_{steer}, W_{throt}$: 각 행동에 대한 가중치
*   $N$: 미니배치 크기 (현재 128)
*   $i: 미니배치의 데이터 샘플 인덱스 번호

#### 2. 가중치 설정 (Weight Configuration)

| 행동 항목 | 가중치 ($W$) | 목적 및 전략 |
| :--- | :---: | :--- |
| **조향 (Steer)** | **5.0** | **경로 추종 핵심**: 미세한 조향 차이가 큰 경로 이탈로 이어지는 것을 방지. |
| **가속 (Throttle)** | **1.0** | **속도 유지**: 속도 변화는 조향보다 낮은 비중. |

#### 3. 핵심 설계 의도
*   **조향 정밀도 극대화**: 조향 가중치를 5배 높게 설정함으로써, AI가 다른 오차는 조금 허용하더라도 **바퀴 각도만큼은 데이터를 완벽히 흉내 내도록** 유도
*   **고주파 진동 억제**: MSE(Mean Squared Error) 특성상 큰 오차에 대해 제곱으로 페널티를 주어, 급격한 핸들링 조작(Jittering)을 최소화하고 부드러운 회전을 유도
*   **데이터 불균형 보정**: 전문가 데이터에서 가속값은 일정하게 유지되는 경우가 많은 반면 조향값은 변화가 잦음. 이를 가중치로 보정하여 조향 학습의 중요도 상승

---

## 시행 영상
- 학습 결과

https://github.com/user-attachments/assets/907857df-8fb5-473b-b58e-59c1c72518b0

- 학습하지 않음(쓰로틀, 스티어링 값을 그대로 실행)

https://github.com/user-attachments/assets/0e744b0a-33d8-45b0-8456-7dd362cc8b0a

- 학습된 MLP를 사용한 유사 경로

https://github.com/user-attachments/assets/69478ac5-9e96-4994-9ea3-c08500d334d3

- 학습된 MLP를 사용하지 않은 유사 경로(쓰로틀, 스티어링 값을 그대로 실행)

https://github.com/user-attachments/assets/f312ebc5-2c4d-425c-b175-629c2949c39a

- 학습된 MLP를 사용한 유사 경로(좌우 반전)
- 지도학습의 한계, 학습된 환경과 유사하지 않은 환경은 혼란을 겪음

https://github.com/user-attachments/assets/23176922-ec50-4e71-a806-def0faebd7ed

- 학습된 MLP를 사용하지 않은 유사 경로(좌우 반전) (쓰로틀, 스티어링 값을 그대로 실행)

https://github.com/user-attachments/assets/851523a2-20bf-44e2-9a0b-962b4f8c3241

